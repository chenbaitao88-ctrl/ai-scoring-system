# 参与开发

欢迎提交可复现的问题和小范围改进。先说明具体操作、预期结果与实际结果，并使用虚构材料复现。

开发前按[运行手册](docs/RUNBOOK.md)安装锁定依赖。涉及业务行为的修改，请增加相应测试，并运行相关测试和前端构建。

几个需要保持的约定：

- 成功评分尝试保留，重试新增尝试。
- 强制复核阻止自动采用；人工最终锁定保护已确认结果。
- 更换 Provider 不同时改变 rubric、Prompt、输入要求或返回格式。
- 不提交真实作品、个人数据、凭据、数据库和运行日志。
- 说明哪些场景已验证，哪些仍需要真实模型或实际环境验证。
